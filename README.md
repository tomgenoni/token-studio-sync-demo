# Tokens Studio sync demo

Minimal example of design tokens, exported from Figma using Tokens Studio plugin, to platform variables using Style Dictionary.

## Parts

- Tokens are in `/tokens`
- Transformation scripts begin with `sd.config.js` and run scripts in `/config`.
